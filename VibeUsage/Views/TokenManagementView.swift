import SwiftUI

struct TokenManagementView: View {
    var body: some View {
        AccountPagePlaceholder(
            icon: "key.horizontal",
            title: "令牌数据准备中",
            message: "令牌列表将在这里安全显示"
        )
    }
}
